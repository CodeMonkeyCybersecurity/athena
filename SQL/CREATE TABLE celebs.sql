CREATE TABLE dateabase (
   id INTEGER PRIMARY KEY, 
   name TEXT,
   age INTEGER,
   app TEXT,
   height INTEGER,
   alcohol TEXT,
   smoking TEXT,
   job TEXT,
   country_of_origin TEXT,
   num_pictures INTEGER,
   num_prompts INTEGER,
   num_emojis INTEGER,
   insta_phone BOOLEAN,
   no_dates INTEGER,
):
  