#include <iostream>

int main() {
  // year as input
  double year = 0;
  double leap_year =0;
  // check if four digits
  std::cin >> "Enter your year in digits: "
  if (year > 9999 || year < 1000) {
    std::cout << "Not a leap year!\n";

    else if (year % 4 == !0) {
      std::cout << "Not a leap year!\n";

      else if ((year % 100 == 0) && (year % 400 == !0) {
        std::cout << "Not a leap year!\n";

         else if (year % 400 == !0) {
    std::cout << "Not a leap year!\n";
  }
  else () {
    std::cout << year << " is a leap year!\n";
  }

  }
  
  }
  
  }
 
}